# 项目代理长期规则

## GLM-5.2 员工调度

当任务属于大规模工作，或步骤简单但耗时较长时，调用项目中已配置的 GLM-5.2 来承担边界明确的子任务，由主代理负责拆解、调度、核验结果和最终决策。

调用时必须严格使用以下唯一命令模板：

```powershell
claude --dangerously-skip-permissions -p "your prompt"
```

使用要求：

- 只能替换两个双引号之间的 `your prompt`，不得改动命令的其他部分。
- 必须保留且只使用这一对双引号包裹完整提示词。
- 不得添加、删除或重排任何命令参数。
- 不得添加管道、重定向、空参数或额外 shell 包装。
- 每次调用前必须机械校验完整命令符合正则 `^claude --dangerously-skip-permissions -p "[^"]*"$`；不符合时不得执行。
- 提示词内部禁止出现英文半角双引号 `"`；需要引用内容时改用中文引号 `“”`、书名号或单引号，确保命令始终只有首尾这一对英文双引号。
- 工具调用界面会把命令内双引号显示为 JSON 转义形式 `\"`。判断是否闭合时，不依据调用卡片的视觉效果；应确认传给 PowerShell 的原始命令末尾为 `"`。若调用异常，再读取实际进程 `CommandLine` 核验。
- 调用后的等待时间必须与任务规模匹配：固定文本约 5-15 秒，中等纯推理约 15-90 秒，代码库审查或大规模工作默认允许 10-30 分钟。
- 长任务不得仅因 30-120 秒内没有最终文本就终止。`-p` 文本模式通常会在整轮代理工作结束后才输出最终答案；期间应每 30-60 秒轮询一次并向用户汇报。
- 判断任务是否仍在进展时，检查 Claude Code 会话 JSONL、进程状态或新增工具事件。只要日志行数、文件大小、CPU 时间或工具事件仍在增长，就继续等待。
- 只有连续至少 5 分钟没有任何日志、CPU 或工具进展，或进程明确报错退出，才可判定 GLM-5.2 调用异常并终止；终止前必须说明证据。
- GLM-5.2 的结果必须由主代理审查，不得未经核验直接作为最终结论或直接接受高风险修改。
